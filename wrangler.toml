# Cloudflare Pages Configuration
name = "vibe-lifestyle-web"
compatibility_date = "2024-01-01"

# Build configuration
[build]
command = "cd webapp && npm install && npm run build"

[build.environment]
NODE_VERSION = "22"

# Output directory
pages_build_output_dir = "webapp/dist"
